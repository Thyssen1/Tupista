.idea/
cmake-build-*/
out/
notes.txt

# Native library staged for the .NET projects to copy; regenerated by CMake.
artifacts/

# .NET build output
bin/
obj/

# Release artefacts: produced by `dotnet publish` and packed for a GitHub
# Release. Regenerable, and a few hundred files of Avalonia binaries that
# would otherwise show up as unversioned every time you cut a release.
publish/
*.zip
